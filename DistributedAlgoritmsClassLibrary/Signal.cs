namespace DistributedAlgoritmsClassLibrary
{
    public enum Signal {
        ACCEPT,
        DECIDED,
        HEARTBEAT_REPLY,
        HEARTBEAT_REQUEST,
        NACK,
        NEW_EPOCH,
        READ,
        STATE,
        WRITE
    }
}
